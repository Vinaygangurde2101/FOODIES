{
  "name": "naik-foods-smartshop",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --prefix client",
    "vercel-build": "npm install --prefix server && npm run build --prefix client",
    "start": "node server/server.js"
  }
}
